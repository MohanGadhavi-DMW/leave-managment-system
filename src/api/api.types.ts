export enum EApiMethod {
    get = "GET",
    post = "POST",
    put = "PUT",
    delete = "DELETE",
    patch = "PATCH",
}

export enum EApiStatus {
    rejected = "Rejected",
    fulfilled = "Fulfilled",
    pending = "pending",
}